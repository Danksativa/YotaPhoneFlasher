============
Installation
============

At the command line::

    $ easy_install YotaPhoneFlasher

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv YotaPhoneFlasher
    $ pip install YotaPhoneFlasher
